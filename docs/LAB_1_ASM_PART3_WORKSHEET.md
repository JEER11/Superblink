Lab 1 – Part 3: Unconditional Branches (Observation Sheet)

Instructions
- Build env `lab1-asm-part3` and upload.
- Start a debug session; set a breakpoint at `asm_main` and step through.
- Record the PC (program counter) after each `j` to confirm it matches the address of the target label.

Checklist (fill addresses observed in the debugger)
- PC at `asm_main` entry: __________
- After `j spot1`, PC = address of `spot1`: __________
- After `j spot4`, PC = address of `spot4`: __________
- After `j spot2`, PC = address of `spot2`: __________
- After `j spot3`, PC = address of `spot3`: __________
- After `j exit`,   PC = address of `exit`:   __________

Note: You can view PC in the debugger’s Registers panel. The Disassembly view will also highlight the current address.